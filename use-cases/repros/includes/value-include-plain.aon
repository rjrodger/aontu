# 85: the working twin of value-include-declares-alias.aon -- the same
# document included as a value, minus the alias declaration.
#
# run: node ../../../ts/bin/aontu.js --trust root value-include-plain.aon
#
# expected: {"g":{"code":{"units":[{"path":"a"}]},"y":1},"z":{"path":"a"}}
# actual:   as expected, in both ports.
g: @"./value-include-plain-inc.aon"
z: $.g.code.units.0
