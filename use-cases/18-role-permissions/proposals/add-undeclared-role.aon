# A role-model edit that adds a fifth role. The registry is close()d,
# so this is a located [aontu/closed] error at $.roles.ops, and
# `aontu allow` answers exit 4 for every role, not only ops.
@"../roles.aon"
roles: ops: { desc:"Operations" allow:["$.deploy"] }
