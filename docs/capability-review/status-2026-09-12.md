# Status report — 2026-09-12

*A dated snapshot of one assessment, not a register.*
[`progress.md`](progress.md) remains **authoritative** for per-phase
status. This file records what an audit of the **module system** found
on 2026-09-12 — the G6 and G10 rows, the six design notes and the
executable specification in `aontu-lang/system`, the transparency client
in `aontu-lang/mod`, the Cloudflare precedent in `aontu-lang/web`, and
the live state on npm, in DNS and on the forge. Where the two disagree,
the register is right and this file is stale.

**This commit changes no register row.** Every regrade it argues for is
written here as a recommendation, because a status changes in the commit
that changes it. The one word it asks the maintainer to move — G10 phase
2, from LANDED to PARTIAL — is §5 item 9.

## Headline

**The registry does not exist yet, and the trust argument the design
rests on is not yet true.** `pkg.aontu.dev` is NXDOMAIN; there is no
registry bucket, no write-path Worker and no registry `wrangler.json` in
any of the four repositories — the only Worker in the tree is the live
docs site. `@aontu-lang/mod` is unpublished, into a scope that answers
404 to an unauthenticated probe. `mod get` and `mod publish` exit 2 in
both ports.

What is real is narrow and green: the local module half in both ports
(G6, five rows LANDED, 43/43 and use case 11 34/34 under both binaries),
a transparency-log verifier that reproduces a live Rekor v2 checkpoint
root from tiles (46/46, 100 % coverage, 720 vectors byte-identical
across two repositories), and an executable specification of the whole
protocol holding 62 checks under the engine it pins.

The design is decided on paper — [ADR-019](../../ADR.md#adr-019--the-project-stores-module-bytes-and-federates-the-log),
[ADR-020](../../ADR.md#adr-020--a-module-path-is-domainpath-and-the-domain-is-a-proved-namespace),
[ADR-021](../../ADR.md#adr-021--the-project-hosts-private-packages-with-authenticated-reads),
[ADR-022](../../ADR.md#adr-022--compatibility-is-computed-so-the-major-leaves-the-name) and
[ADR-024](../../ADR.md#adr-024--the-forges-token-authorises-a-publish-and-sigstore-is-one-provider-of-the-proof-not-its-definition),
all between 2026-09-04 and 2026-09-06 — but conditional on two things
that are false today: the client-verification contract lives in the
private `system` repository the split rule forbids, and an operator key
has returned four ways after being declared retired.

**One new defect sits on the integrity path**, recorded as
[`use-cases/BUGS.md`](../../use-cases/BUGS.md) §95. The two ports render
one declared conjunct three different ways, so they compute different
canon-hashes for the shape the specification is built on, and a lockfile
written by one port fails the other's `mod verify` over an untouched
store. Beside it, a defect
carried since [`status-2026-09-05.md`](status-2026-09-05.md) and
unchanged at HEAD: the `mod` verbs evaluate vendored third-party modules
at the default `system` capability while refusing `--trust`.

## 1. The claim, checked

Seven properties a user of this registry would be asked to rely on.
A gap that is not on this list is housekeeping.

| # | Property | Status |
|---|---|---|
| 1 | The client verifies before it trusts: signed manifest, accepted signer per name, inclusion proof | **Specified, one of three built.** `mod` verifies log inclusion and nothing about the bytes — no manifest or digest handling anywhere in `mod/src/` — and it is unreachable: npm 404, `mod get` exit 2. ADR-024's decision, part 2. |
| 2 | A pin that means the same thing in both ports: archive digest before parse, canon-hash after evaluation | **Built and broken, plus unbuilt.** No `archive` symbol in `ts/src/`, so the cheap pre-parse pin does not exist; the surviving pin computes two different values across the ports (§2). ADR-019's consequences. |
| 3 | A publish only the namespace owner can make, from the forge's OIDC token and never a certificate | **Specified, unbuilt.** ADR-024's decision, part 1. |
| 4 | Stored bytes cannot be silently altered or rolled back | **Specified, unbuilt.** `spec/publish.aon:52,67` place "alter a published version" under `never:`; no write path stands behind them. |
| 5 | A name and a read path that survive one account action | **Unwritten.** The exit needs a registrar held elsewhere (done) and a mirror (none). `pkg.aontu.dev` does not resolve, so there is no read path for an account action to survive. |
| 6 | An operator a notice can reach | **Unwritten, and countable at zero.** `REPOSITORY.0.md` §7.4 requires five items before the first publish. The only contact in the tree is a GitHub advisory URL that needs an account, carries no email, and still points at `rjrodger/aontu` (`SECURITY.md:32,35`). |
| 7 | Freshness | **Unwritten, and self-declared as the gap.** "Rekor inclusion does not help: it proves a version exists, not that it is the newest… this design has no equivalent". Both mitigations unbuilt: no `cooldown` or `firstSeen` symbol in either client. |

None is fully met. One is built and broken, three are specified and
unbuilt, three are unwritten.

## 2. The finding that moves the priorities

**The canon-hash is port-dependent at HEAD, and nothing records it.**

On `spec/registry.aon` in `aontu-lang/system`:

| Engine | `aontu hash --trust root:spec spec/registry.aon` |
|---|---|
| TypeScript 0.57.0, 0.58.0, 0.59.0 | `aon1-yVCc2Q9gxIWwKB5rocsSWcuBxtd2g8nE3qX6w20yykE` |
| TypeScript 0.60.0, 0.61.0, 0.62.0 | `aon1-wyMpq-lY4AGtctwe5f5zup8CcItpieNNKjDR54IiKBw` |
| Go v0.1.15 through v0.1.20 | `aon1-yVCc2Q9gxIWwKB5rocsSWcuBxtd2g8nE3qX6w20yykE` |

The difference is one conjunct in the element type of a list spread
template. It reduces to six lines, recorded as
[`use-cases/BUGS.md`](../../use-cases/BUGS.md) §95 with its
reproduction, and there the divergence is three-way:

```
TS 0.59.0   "refuse"?:[&:refer()&refer()&path(), …]
TS 0.62.0   "refuse"?:[&:refer()&refer(), …]
Go 0.1.20   "refuse"?:[&:refer()&path(), …]
```

The source declares that element type once, as `refer() & path()`.
**Go renders the declaration. Both TypeScript releases double the
`refer()`, and 0.60.0 additionally drops the `path()`.** The trigger is
the spread sitting behind another constraint atom: remove the
`unique(n)` in front of it and all three agree. On the specification it
occurs at 43 sites.

**It is not a lost constraint.** Acceptance is unaffected. Both ports
refuse a non-address and a dangling address at this element with the
same codes, and under TypeScript 0.62.0 the specification's gate is
61 of 62 with the pin as the single failing check, so all 39 vector
verdicts hold — the negative ones, with their codes, included. Nor has
TypeScript stopped discriminating `path()`: strike the conjunct from
`spec/schema.aon` and TypeScript's hash moves too, to a value Go agrees
with. What differs is where the conjunct is placed in the rendered
form, not whether it is carried.

**Why it survived.** `test/spec/hcanon.tsv` holds 53 rows and not one of
them mentions `path(`. The parity ledger `test/spec/divergent.tsv`
carries four OPEN entries, none of them this, and by design contributes
no executable rows — it is a file of comments read by both runners. So
both CI matrices are green over the divergence.

**What it costs.** A lockfile written by Go's `mod tidy` fails
TypeScript's `mod verify` on an untouched store, for the exact shape the
registry specification uses. ADR-001 promises the two implementations
are one; on the integrity pin they are not. Any pin the registry stores
before this is fixed is a pin one port will reject.

**Which form is right.**
[ADR-001](../../ADR.md#adr-001--typescript-and-go-stay-at-full-parity-driven-by-a-shared-spec)
makes TypeScript canonical *where neither is obviously broken*, and that
qualification decides it: a conjunct written once and rendered twice is
broken on its face, and both TypeScript releases do it. Make TypeScript
render what the declaration says, as Go does. Pin the duplicate and the
dropped conjunct with shared `hcanon` rows before either port moves,
since a row cannot exist until both ports agree.

Fix the engine rather than writing a ledger entry. Both renderers are in
this repository (`ts/src/hcanon.ts`, `go/hcanon.go`) and depend on
nothing pinned, which is neither excuse `divergent.tsv:18-33` admits.
Go's form is the one `spec/hash.txt` already carries, so the
specification re-pins only if the fix moves Go as well.

## 2a. What a pin promises, given bundled models

A second, quieter problem sits underneath the first. Eight bundled
models — `aontu/system`, `view`, `code`, `render` and four `render/lang`
profiles — are inside every canon-hash a module carries: a two-line
document that includes `aontu:system` hashes with a 1,516-byte form
against 8 bytes for `x: 1`, identically in TypeScript 0.62.0 and Go
v0.1.20. Every one of the eight is still headed *"EXPERIMENTAL until the
distribution layer can version it by canon-hash"*, and the set went from
zero to eight with a rename in six days. Nine pin moves landed in five
days, eight of them model *content* rather than hash form; more are
queued in the unreleased section, one marked BREAKING.

A scheme-id bump cannot address this class — a registry cannot mint
`aon2-` because a vocabulary gained a field. `T.LockEntry` is closed over
four pins with no engine identity, so `lock_mismatch` cannot distinguish
"the package changed" from "your engine changed", and `refreeze` exists
in four design notes and in neither port. `mod tidy` is not refreeze:
`ts/src/mod-tool.ts:246` overwrites the stored pin with whatever the
current engine computes.

**Both pins stay mandatory.** ADR-019 requires the archive digest before
parsing and the canon-hash after evaluation, and calls a client that
checks only the cheap one a defect. Nothing here weakens that, and it
should not be weakened by drift: an archive digest proves the stored
bytes are unchanged and cannot see those same bytes changing meaning
under a different engine or a different bundled set, which is precisely
the failure above. What is not yet true is that the canon-hash is
*portable* — between the ports today, and between engine versions
whenever a bundled model moves. Fix that where it belongs: engine
identity in the lock entry, so a mismatch can say which of the two
changed, and a stable bundled set under its own ADR. Demoting the pin
would need an entry superseding ADR-019, and nothing here argues for
one.

## 3. Where each piece stands

| Area | Built / specified / designed |
|---|---|
| Engine, local module half (G6) | **Built**, both ports, green at HEAD `5e03591`. Implements the August design: `MODULE_RE` requires `@<major>` (`ts/src/mod.ts:22-23`, `go/mod.go:22-23`), reads `mod.aon`, lock entry `{v, canon, oci}` with `oci` never computed, cache keyed by canon-hash alone, no file manifest, MVS built. |
| Engine, network verbs (G10 phases 3, 4, 6) | **Not started.** `mod get` and `mod publish` refuse with "needs a registry client" (`ts/src/cli.ts:2121-2126`, `go/cmd/aontu/mod.go:65-70`). No `ModuleFetch` seam, `module_moved`, cooldown, `alias:`, `./` rule, or any Sigstore or OIDC symbol in either port. |
| Engine, trust contract over the `mod` verbs | **Built wrong, carried since 2026-09-05.** `runMod` never calls the trust parser; `--trust` and `--include-root` are refused as "unknown mod option" in both ports (`ts/src/cli.ts:2109-2112`, `go/cmd/aontu/mod.go:48-50`), while the global help at `ts/src/cli.ts:154` and `docs/trust.md:230-232` say every verb takes them. Vendored third-party modules are evaluated at `system` capability. |
| Engine, publish gate (`mod manifest --against`) | **Built, two of three components**, all reproduced at HEAD. A changed effective default gives `verdict: breaking`, exit 1. `port: 8080` → `port: integer` gives `verdict: ok`: ADR-022's determination component is absent. A major bump skips the gate entirely (`ts/src/mod-tool.ts:539-541`), which under ADR-022's no-major rule inverts into a bypass. Still emits an OCI artifact for a channel ADR-019 foreclosed. |
| Cross-port canon-hash parity | **Divergent**, three ways, on the pin itself. §2, and [`use-cases/BUGS.md`](../../use-cases/BUGS.md) §95 with its reproduction. |
| `aontu-lang/mod`, the tlog client | **Built, unpublished.** 46/46, 100 % coverage, `x/mod` v0.32.0 pinned, vectors re-derive byte-identically. Live: it verifies a real Rekor v2 checkpoint and reproduces the root from four tiles. Ed25519 only; no fetch, bundle, certificate, DSSE or TUF. Tile paths are Go-sumdb shaped, which the live Rekor v2 log answers 404 to. No publish workflow, no tags, no `repository` or `publishConfig`, no `prepublishOnly`. |
| `aontu-lang/mod`, the owed specification | **Absent.** ADR-024 requires the proof contract's normative text there, beside the leaf and checkpoint specification the G10 split already places there. The repository holds code, vectors and licences; there is no `docs/` or `spec/`. |
| `aontu-lang/system/spec` | **Specified, gated.** Seven operations, 67 steps, 49 codes, 39 vectors, 13 schema exports; `npm test` is 62/62 under the pinned `aontu@0.57.0` and under every Go release from v0.1.15 to v0.1.20. Under the engine at HEAD it fails exactly once, on the pin. It holds `T.Proof`, `T.TrustConfig`, `ops.acquire` and `ops.verify` — the client contract — in the repository the split rule forbids, by its own admission. Status: proposal. The repository is private. |
| `aontu-lang/system` design notes | **Designed.** `REPOSITORY.0.md` is a proposal on an approved decision; its cost model uses rates the note itself flags as copied from third-party summaries and unverified; its P0–P5 sequence is unamended by ADR-024. `LEGAL.0.md` is "a review rather than a decision", counsel has not reviewed it, and the memorandum it reads is in no repository. `PATCHING.0.md` is 1,796 unreviewed lines with no upstream counterpart. The repository has no ADR file and no LICENSE. |
| Both providers | **Designed only.** ADR-024 places the Sigstore provider and the key provider behind the phase-3 fetch seam. The verifier choice is undecided against the project's dependency posture. `ed25519Verify` exists in `mod` but is not exported. The bundle-to-contract mapping — which `logIndex` `T.Inclusion.index` means, what is hashed as the leaf, where the saved checkpoint lives, whether Rekor v1 bundles are refused — is unspecified. |
| `pkg.aontu.dev` | **Nothing deployed.** NXDOMAIN, no bucket, no Worker, no deploy path decided, no fifth repository. Named in a proposal note and in `spec/layout.aon:10`. No ADR names the host or the storage provider. |
| `aontu.dev` hygiene | **Partly done.** Registrar Gandi SAS, which satisfies "not Cloudflare" by a registration of 2026-08-10 that predates the 2026-09-04 rule. One-year term to 2027-08-10; `clientTransferProhibited` only, not registry lock; DNSSEC undelegated; no CAA; registrant not visible. Plain `http://aontu.dev/` answers 200 with no HSTS. |
| Cloudflare precedent (`aontu-lang/web`) | **Built, live** on 0.62.0. One Worker with an ASSETS binding and two custom domains, deployed on merge to `main` by Cloudflare Builds with no deploy workflow, a reused account build token, and tests that never run in CI. It is not the read-path precedent: the registry read path has no Worker. |
| The project's own release provenance | **One channel of thirteen carries any.** npm `aontu` has a SLSA v1 attestation naming `publish.yml`, which is the same *kind* of control ADR-024 rests on: a build identity attested from the workflow's OIDC token. It is not the ADR-024 proof, and should not be counted as coverage of it — that requires a signed aontu manifest carrying the archive digest, the file manifest and each module's canon-hash, a signer the trust configuration accepts for the name, and inclusion where required. None of that is produced anywhere today. The container image sets `provenance: false`. Release binaries and `install.sh` verify a `SHA256SUMS` fetched from the same origin, with no signature, and every downstream package manager inherits that. Both GitHub Actions are documented for consumption at `@main`; every third-party `uses:` is a floating tag. Three editor packages are published by hand over a PAT. |
| Legal prerequisites | **Designed.** No security.txt, no SBOM, no CRA date check, no legal person, DMCA agent, terms, licence field or Article 16 form. `ADR.md` contains no legal, DMCA, GDPR, CRA or registrar word. One hidden dependency: `LEGAL.0.md`'s e-Evidence preservation answer rests on "there is no overwrite operation", which is a property of unwritten Worker code and an unconstrained R2 credential, not of anything that exists. |
| The register and the gap documents | **Partly stale, and narrower than it first reads.** G10 rows 3 and 4 have already been rewritten for the federated design: row 3 around the static repository and its two ordered pins, row 4 around token verification under ADR-024. **Only row 6 still plans for a log the project will not run** — cosignatures, checkpoint gossip, the mirror. Elsewhere the drift is real: ADR-022 is cited by no review document, `progress.md` says the naming rule needs no code while `MODULE_RE` still requires a major, "Corrections outstanding" declares itself empty, nothing in `progress.md` references `system/spec` at all, and no issue in any of the four repositories mentions the registry. |

## 4. What is decided, and what is only provisional

Settled, and not to be reopened without a new entry: bytes stored and
the log federated (ADR-019); the read path a static, GOPROXY-shaped
bucket, mirrorable by copy; two pins in a specified order; provenance as
forensics rather than a control; moderation shipping *with* publish;
`<domain>/<path>` names never resolved, with the subject pair as the
signing identity (ADR-020); no major in the path and compatibility
computed in three components (ADR-022); the private tier (ADR-021); and
the token authorising while Sigstore merely provides the proof
(ADR-024).

Two of those need a footnote the documents do not carry:

- **"No code on the read path" is true of the objects only.** The same
  design assigns the read path three active edge behaviours — path-shape
  rejection before an object read, one-shot 404 caching, and zone rate
  limiting (`spec/layout.aon:43-47`). Those are Cloudflare rules living
  in an account with no repository, no review and no deploy path.
- **"Provenance is forensics" does not weaken property 1.** The control
  is ADR-024's proof contract; Sigstore is one provider of it. What a
  user is asked to trust is the contract, not the log.

Dated in `aontu-lang/system` with no ADR, and unadopted in the engine:
`pkg.aontu.dev` as a base URL; the registrar held away from Cloudflare;
`publish` at top level with `add` and `get` both existing;
`mod.aon`/`mod-lock.aon` becoming `pkg.aon`/`aontu_meta/pkg-lock.aon`;
and no migration shims. Three naming schemes now coexist across the
engine, the specification and the notes.

The nine provisional choices in `spec/params.aon` are all ratifiable now
except the durations. `lock_pins` should gain a fifth field — the engine
identity, which `aontu mod manifest` already emits — and the byte pin's
name should be settled at `archive`, since the engine calls it `oci`,
the specification calls it `archive` and `PATCHING.0.md` calls it
`integrity`. The thirteen numbers can stay provisional; they become
commitments the moment the first archive is stored.

**Eight spec choices carry security weight and are not marked
provisional**, which `registry.aon:20-23` makes the rule. The worst is
the advisory object: `/advisory/<package>.aon` is mutable on a
60-second TTL, unsigned, with no expiry and **no timestamp of any
kind**, so it is the one mutable object a client cannot date. Nothing in
`ops.acquire` verifies it, the rollback check protects the version list
and has no analogue for it, and retraction has exactly one carrier —
this file. The commissioned survey asked for a signed, append-only
advisory; the specification renders it unsigned and overwritten in
place.

## 5. Open decisions

Ordered by what they block, not by size. The full argument for each is
in the sections above.

1. **The canon-hash rendering rule.** §2. Blocks the engine pin move in
   `system`, the `refer() &` restoration on `Step.phase`, and the
   portability of every stored `aon1-` pin. Take TypeScript's form per
   ADR-001 unless a discriminating `hcanon` row can be written.
2. **What a canon-hash pin promises, given bundled models.** §2a. Keep
   the status quo but make it honest: retire the EXPERIMENTAL headers
   under an ADR when the set stops moving, store the archive digest as
   authoritative meanwhile, add the engine-identity field, and specify
   `refreeze`.
3. **Key custody for the operator key federation was said to retire.**
   It has returned four ways: the tombstone signature, the key provider,
   a signed expiring `@v/list`, and the advisory the survey asked to be
   signed. One ADR covers all four. Recommended: the write-path Worker
   holds no signing key; tombstones are signed offline by a named human
   as a runbook step; the key provider is limited to publisher-held keys
   on the public tier; the signed `@v/list` is declined for v1; and the
   residual credentials are enumerated before P2.
4. **The Sigstore provider: which verifier, and how a bundle maps onto
   the contract.** Specify the mapping in the `mod` contract first.
   Refuse Rekor v1 bundles explicitly. Do not let this block the key
   provider or the self-publish milestone.
5. **Where the contract lives, and which way the dependency points.**
   ADR-024 says `mod`; it is in `system`. Move it as gated aontu
   documents with an engine devDependency **pinned to a released
   version, never HEAD** — running the same gate against HEAD fails on
   the hash today, which is exactly what the pin exists to prevent —
   and publish `mod` before the engine consumes a new contract, so a
   contract edit is two pull requests in a fixed order.
6. **Which legal person operates the registry and holds the account.**
   The longest pole; no code shortens it. The entity does not block the
   first measurement, but the *account* choice blocks P1: the R2 custom
   domain requires the zone in the Cloudflare account, and that zone
   already sits in an account shared with another site, so deciding
   after creating the bucket means a DNS cutover of the live docs site.
7. **Off-account backup: provider, cadence, holder, and what it does
   with a tombstone.** Entirely absent — three grep hits across the
   notes, no destination, no holder, no row in the 27-row gap table —
   while the stated emergency remedy is to serve from a mirror and there
   are no mirrors at launch. Attach a copy to P1, where the corpus is
   334 KB of first-party content and no publisher licence or
   notice-and-action question attaches yet.
8. **Explicit email-SAN refusal**, which ADR-024 implies structurally
   but no code, vector or ADR text states. One publish step.
9. **G10 phase 2's status word**, and the mechanism for the first
   `@aontu-lang/mod` publish. AGENTS.md settles the word: a phase is
   landed only when both ports have it and shared rows pin it, and this
   one exists in Go alone with no shared `tlog` rows. Publishing the
   package does not land it either; the shared rows do. So grade PARTIAL
   in the record-hygiene step below, and restore LANDED in the commit
   that adds those rows. What is genuinely open is only the first-publish
   mechanism, since the OIDC-only release rule has no recorded answer for
   a new scope. Claiming the npm scope is not part of it and depends on
   nothing.
10. **The `mod` verbs under the trust contract.** Fix, do not narrow the
    documentation: these verbs become `sync` and `verify`, and must not
    carry the hole forward.
11. **Ratify the naming with an ADR here.** CLI.0's four decisions plus
    ADR-022's engine consequences. CLI.0's "no shims because nothing is
    public" premise is contradicted by the tree: 0.53.0 and later are on
    npm, hand-vendoring is the documented cold start, and both ports
    already carry a refuse-and-name shim for the `aontu_meta/` move.
12. **Freshness, and what covers the advisory.** First-seen only for v1,
    stated as a known gap; sign the advisory once item 3 produces a key.
13. Smaller, each argued above: private-tier provenance and phase; the
    escape hatch for an intended outcome change; the `aontu-release/v1`
    leaf, which ADR-024 supersedes; the `system` engine pin move, whose
    Go half can land today; the write-path deploy path and the
    credential scope at bucket creation; the compat gate at P2 and the
    fate of `mod manifest`; the legal-surface choices; the project's own
    namespace; the survey's unanswered bundling contradiction; the
    gap-document rewrite; `PATCHING.0.md`; and the use-case corpus under
    Go in CI.

## 6. Next steps, in dependency order

Steps 1 to 7 and 9 run in parallel from day one.

1. **Claim the `@aontu-lang` npm scope.** Minutes. Depends on nothing,
   and mitigates a squatting risk that is live today.
2. **Fix the cross-port canon-hash divergence**, recorded as BUGS.md
   §95. Make TypeScript render the declaration; pin both the duplicated
   `refer()` and the dropped `path()` with shared `hcanon` rows over a
   spread standing behind another atom; release both ports; decide the
   scheme-id policy. Days.
3. **Record hygiene in one commit.** G10 phase 2 to PARTIAL — restored
   to LANDED by step 11, which adds the rows that land it — owners for
   the three P0 items, ADR-022 rows in G6 and G10, fill "Corrections
   outstanding" (row 6's log plan, not rows 3 and 4), refresh the
   release paragraph and the suite counts. Hours.
4. **One ADR ratifying naming and the pre-storage provisionals.** Hours.
5. **The two measurements that decide the topology:** whether Cache
   Rules engage in front of R2, which the note ranks first in urgency,
   and whether a worst-case conforming publish fits one Worker
   invocation, which nothing currently asks. A day, and neither needs
   anything that does not exist.
6. **Domain hygiene, everything but the registrant name.** Hours. This
   precedes the first resolve, not the first publish.
7. **Counsel, and the hygiene that needs no counsel.** A day for the
   files; weeks, external, for counsel.
8. **Decide which account and which holder owns the zone and the
   bucket.** Strictly before P1.
9. **Bring the `mod` verbs under the trust contract**, both ports. A day.
10. **Decide the cycle rule, then publish `@aontu-lang/mod` 0.1.0.**
    Settle the tile-path target or leave `tilePath` unexported first, so
    the first public version does not ship a known-wrong path API.
11. **Add the shared `tlog` spec mode** in both runners. A day.
12. **Move `system`'s Go pin to v0.1.20 now**; the TypeScript half waits
    on step 2. Hours.
13. **Move the engine to the specification's shapes**, both ports. One
    to two weeks. Depends on steps 2 and 4 — every artifact it re-pins
    is a stored `aon1-` value, so doing it before step 2 means re-pinning
    everything twice.
14. **Move the client contract into `mod`** as gated aontu documents,
    specifying the seam. A week.
15. **Build the key provider.** A week. This is the provider the
    self-publish milestone needs; Sigstore does not gate it.
16. **P1: the R2 bucket on `pkg.aontu.dev`, with no Worker** — cache
    classes, path-shape rejection and 404 caching at the edge, a
    create-only write credential from the first day, and a scheduled,
    restore-tested off-account copy. A day.
17. **P4's client-side controls, which the evidence rates above P2** —
    the trust set at resolve time, and `aontu diff` as a verb, since the
    specification names the semantic diff as one of only two claimed
    defences against publisher compromise and it is reachable from the
    library and the MCP server but not the CLI. Days.
18. **Register a PURL type.** One pull request, and the cheapest open
    CRA item.
19. **Build `get`, `sync --frozen`, `add`, `remove`, `why` and
    `publish`** behind a total `ModuleFetch` seam, running the
    specification's vectors through the verbs. Two to three weeks.
20. **The determination component and the escape hatch.** Days to a week.
21. **Decide the write-path deploy path** and enumerate the remaining
    Worker credentials. A day, before P2.
22. **The legal residue.** Weeks, mostly external. ADR-019's moderation
    subset gates even a self-publish, so this is on the milestone's path.
23. **P2 with P3: the write-path Worker** — the 25 steps of
    `publish.aon`, plus retract, tombstone and feed. Two weeks.
24. **Milestone, running:** the project's own package published from CI
    under the key provider and acquired from a clean machine by both
    ports, pins checked in order. A day.
25. **The Sigstore provider.** One to two weeks. Gates opening to third
    parties, not the milestone.
26. **Rewrite G10 row 6 and the gap documents** for the federated
    design, in the phase-3 commit. Rows 3 and 4 are already rewritten
    and carry the current fetch and trusted-publishing requirements, so
    this is row 6's log hardening and the gap documents' prose, not a
    wholesale rewrite. A day.

There is one critical path, not two: 2 → 12 → 13 → 14 → 15 → 19 → 23 →
24, with 7 → 22 → 23 running alongside it and plausibly longer. Step 13
is on it, not beside it: step 19 runs the specification's vectors
through the new verbs, and until the engine carries the specification's
shapes those verbs would have to target the contract ADR-020 and ADR-022
replaced.

## 7. Risks

- **The integrity pin is port-dependent today**, and both CI matrices
  are green over it.
- **Stored pins have no upgrade story**, and the moving part is not one
  a scheme id can fix: eight of the last nine pin moves were bundled-model
  content.
- **One open engine defect still sits on the hash path.** `BUGS.md` §82:
  a module whose vocabulary uses a recursive alias is pinned by a canon
  that does not re-evaluate to itself, identically in both ports.
- **Key custody was declared retired and has returned four ways**, while
  the deploy path for a Worker holding credentials is undecided and the
  only precedent deploys whatever merges to `main`.
- **Verification by the client is the whole integrity argument, and the
  client verifies no bytes yet.** The contract lives in a private
  repository the split rule forbids, so "independently auditable client"
  is false today, and a phase-3 implementer following the register finds
  nothing in `mod`.
- **Retraction has one unsigned, undateable carrier.**
- **The `mod` verbs evaluate vendored third-party modules unconfined**,
  and ADR-022 puts an evaluator on the write path whose only declared
  bounds are passes, revisits and depth. On the write path the real
  bounds come from the platform, and nobody has measured whether a
  worst-case publish fits one invocation — so the denial-of-service
  question and the topology question are the same unanswered question.
- **The read path's security controls live where nothing in these
  repositories can see them.**
- **The design declines to defend the dominant real attack** — a
  compromised publisher account or CI — and the one cheap control the
  evidence rates highest among the unbuilt, the trust set at resolve
  time, appears in no register row and no spec step.
- **The project requires of publishers a provenance it achieves on one
  of its own thirteen channels.**
- **Nothing legal exists**, and the e-Evidence preservation answer rests
  on a property of unwritten code and an unconstrained credential.
- **Account and domain concentration, with no copy anywhere.** A
  suspension takes the read path, the name and the only copy together,
  despite ADR-019's stated exit.
- **Cost is unbounded under attack**, and both the $20 column and the
  cache-miss arithmetic rest on rates the note itself flags as
  unverified.
- **Sigstore's shape is moving under the design**: the verifier is
  Ed25519 only, witness cosignatures on Rekor v2 checkpoints cannot be
  verified, and `x/mod` is nine releases behind with no scheduled review.
- **The engine and the specification disagree on the name of every
  module**, and each release accretes fixtures against the shape ADR-022
  removed.
- **The record is drifting faster than it is corrected.** Six ADRs in
  eight days reversed one axis twice; the register still plans OIDC
  binding and cosignatures for a log the project will not run; the
  commissioned survey is cited twenty times and tracked nowhere; the
  specification is held to 0.57.0 while users install 0.62.0; and no
  issue in any repository mentions the registry, so an unattended agent
  or a new contributor cannot find the work.

## How far to trust this

Every claim above was held to one evidence rule: a path with a line
number, a command with its output, a commit, or a URL. Each was then put
to an independent pass whose brief was to refute it.

§2 is reproducible from this repository. The commands are:

```sh
# the divergence, minimised
aontu hash --form use-cases/repros/hash/conjunct-behind-a-spread.aon

# acceptance is unaffected: 61 of 62, the pin the only failure
cd ../system && AONTU=<a 0.62.0 binary> node spec/check.mjs
```

**This is written by the agent that did the work, and has no human
verifier.** Claims about state outside these repositories are soft and
are marked where they appear: the registrar record, the npm scope probe,
the cost rates the design note itself flags as unverified, and the legal
memorandum, which is in no repository.
